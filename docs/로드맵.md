# IRIS 메신저 — 폴더·세션별 작업 순서 (2026-09-12 사용자 확정)

Face 콘센트(P02, v2.47.0 계약 v1 + v2.47.1 보조 완료 판정 수정)는 끝났다. 남은 것은 아래 순서대로.

| 순서 | 어디서(폴더·세션) | 무엇을 | 첫 요청 문장 | 사용자 손 |
|---|---|---|---|---|
| 0 | Face 창 | ⏻ 전부 종료 → 재실행(콘센트 적용, 노란불 정리) | — | ⏻ |
| 1 | P02 Face 폴더의 다른 세션(별 애니메이션 v2.48) | origin main(v2.47.1) 위로 rebase 뒤 푸시 | "origin main이 v2.47.1로 앞서 있어. rebase하고 푸시해" | 없음 |
| 2 | 브라우저(사용자) | Supabase 무료 프로젝트 1개(리전 Seoul) · Resend 가입 → `C:\IRIS\_agent\claude\secrets\.env`에 `SUPABASE_URL`·`SUPABASE_ANON_KEY`·`SUPABASE_SERVICE_ROLE_KEY`·`RESEND_API_KEY` | — | 값 4개 적기 |
| 3 | **이 폴더(P04) 새 세션 ①** | `git init`·`.stack`·`.supa` 바인딩·remote `d06-p04-iris-messenger` + `설계.md` 조각 2~10 → 구현계획 | "메신저 구현 시작. 먼저 저장소·스택 바인딩하고, 설계.md 조각 2~10으로 구현계획을 써" | 스택·Supabase 계정 이메일 답 |
| 4 | P04 같은 세션 | ② 우체국 `hub\server`: 표 5·RLS·초대 수락 함수·30일 청소·처리방침 → Supabase 배포 → `verify-rls` | "구현계획대로 서브에이전트 방식으로 진행" | 없음 |
| 5 | P04 같은 세션 | ③ 이용권 `hub\client`: 6자리 코드 로그인·DPAPI 토큰·12단어 열쇠·E2EE·초대 코드·연락처 고정 | (자동 이어감) | 시험 메일 1통 수신 확인 |
| 6 | P04 같은 세션 | ④ 가전 `module`: 편지 흐름·오프라인 보관·파일·화면·알림·번들·서명 zip 릴리스 | (자동 이어감) | 없음 |
| 7 | P02 Face 세션 또는 Face 설정 화면 | 서명 zip 설치 → 헤더 💬 → 로그인 → 혼자 왕복 시험 | "P04에서 만든 zip을 설치하고 왕복 시험해" | 이메일 코드 입력·12단어 적어두기 |
| 8 | 상대방 PC | Face 설치(P03 설치 패키지) → 같은 zip 설치 → 초대 코드 교환 → 2주 실사용 | — | 상대 섭외·코드 전달 |
| 9 | P04 새 세션 ②(2주 뒤) | 조각 9 운영 점검(한도·백업·깨우기) + 2차(에이전트 소통) 설계 착수 | "실사용 2주 결과 정리하고 2차 설계 시작" | 결정 6개 |

요점: Face 폴더는 1·7 두 번만. 코드는 P04 한 세션에서 3→6 연속(콘센트와 같은 과제별 검토 방식). 4는 2의 값이 없으면 시작 못 함. 설계 정본 = `설계.md`, 콘센트 계약 정본 = P02 `docs\모듈-계약-v1.md`, 시험 모듈 = P02 `scripts\fixtures\hello-module`, 서명 도구 = P02 `scripts\sign-module.mjs`(개인 열쇠 secrets `iris-module-signing-2026-09.key`).
